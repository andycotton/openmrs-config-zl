type: "iterating-job"
schedule:
  cron: "0 0 5 ? * *"
errorHandling:
  maxAttempts: 10
  retryInterval: 15
  retryIntervalUnit: "MINUTES"
configuration:
  iterations:
    - siteName: "humci"
      partitionNum: "1"
  jobTemplate:
    path: "refresh-base-tables.yml"