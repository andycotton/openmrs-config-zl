type: "iterating-job"
schedule:
  cron: "${executeCron.refreshHumciData}"
configuration:
  iterations:
    - siteName: "humci"
      partitionNum: "1"
  jobTemplate:
    path: "refresh-base-tables.yml"
